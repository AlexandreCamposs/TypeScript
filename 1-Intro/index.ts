const nome = 'alexadre';
const idade = 30;
const boolean = true;

function test(n: string): void {
  console.log('Olá ' + n);
}

test(nome);

// test(x);

// 1-Desafio

const numOne = 5;
const numTwon = 10;

function sum(a: number, b: number) {
  return a + b;
}

console.log(sum(numOne, numTwon));
